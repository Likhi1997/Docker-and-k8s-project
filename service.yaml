apiVersion: v1
kind: Service
metadata:
  name: zomato-service
spec:
  selector:
    app: zomato-food
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 30001